import { SearchContainer, SearchInput } from './styles'

export default function Search() {
    return (
        <SearchContainer>
            <SearchInput type="search" placeholder='Pesquise um filme' />
        </SearchContainer>
    )
}